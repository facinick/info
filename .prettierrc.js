/**
 * Prettier is used as a plugin inside eslint, running eslint will apply
 * prettier to the code as well.
 * Restart Editor after making any changes here to see them into effect
 * All these config are applied via eslint prettier plugin
 */
module.exports = {
    semi: true,
    trailingComma: 'all',
    singleQuote: true,
    printWidth: 120,
    tabWidth: 4,
    proseWrap: 'always',
    arrowParens: 'avoid',
    bracketSpacing: true,
    endOfLine: 'lf',
    jsxBracketSameLine: false,
};
